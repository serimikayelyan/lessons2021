#!/bin/bash

if [[ $# -ne 3 ]]; then
    echo "You need to specified 3 arguments"
    exit 1
fi
a=$1
b=$2
c=$3
if [[ $a -lt $b && $a -lt $c ]]; then
    if [[ $b -lt $c ]]; then
        echo "$a $b $c"
    else
        echo "$a $c $b"
    fi
elif [[ $b -lt $a && $b -lt $c ]]; then
    if [[ $a -lt $c ]]; then
        echo "$b $a $c"
    else
        echo "$b $c $a"
    fi
else
    if [[ $a -lt $b ]]; then
        echo "$c $a $b"
    else
        echo "$c $b $a"
    fi
fi
