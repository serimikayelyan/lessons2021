#!/bin/bash


arr=( 2 4 13 54 26 9 90 13 32 )
max=${arr[0]}
min=${arr[0]}
     for i in ${arr[@]}
       do
	  if [[ $i -ge $max ]]
  	    then max=$i
	  fi
       done
     for j in ${arr[@]}
        do
  	  if [[ $j -le $min ]]
	    then min=$j
	  fi
       done
echo $(($max*$min))
